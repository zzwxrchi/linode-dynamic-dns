Build and install app
	https://dzone.com/articles/executable-package-pip-install

	After pip install linode-dynamic-dns, cp to /usr/local/bin
		python3 -m pip install dist/linode-dynamic-dns.whl
		sudo cp ~/.lcoal/bin/linode-dynamic-dns /usr/local/bin/

Host
	Create hosts in linode first

IPv6
	remove host's IPv6 in linode, if don't soport IPv6 in local server
